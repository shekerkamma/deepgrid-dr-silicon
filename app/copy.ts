/** Shared standing copy. PRE_SILICON appears on every page that states a figure, so it
 *  lives in one place: a redesign is exactly where a disclaimer quietly stops appearing. */
export const PRE_SILICON =
  'Pre-silicon. Figures are design values verified in simulation and static timing, not measurements on fabricated parts, unless marked otherwise.';
